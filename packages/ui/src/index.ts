// Shared component primitives (Button, Card, skeleton loaders, ...),
// consumed by apps/web, apps/site, and eventually apps/mobile — see
// Platform Blueprint, Section 15. Empty on purpose: apps/web keeps its
// existing src/components/ui/ for now, and components migrate here
// deliberately, one at a time, once this package is ready to be a real
// dependency rather than a workspace placeholder.

export {};
